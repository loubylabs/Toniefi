fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
python-multipart==0.0.20
# Deliberately unpinned. yt-dlp ships fixes within days of a site changing its
# player, so a pin here is a time bomb: an old release fails every YouTube URL
# with "The page needs to be reloaded" and nothing else in the app can tell you
# why. Rebuild the image to pick up a newer one.
yt-dlp>=2026.8.19
